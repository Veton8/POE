[gd_scene load_steps=4 format=3 uid="uid://b5hitvfx001"]

[sub_resource type="GDScript" id="gd_hit"]
script/source = "extends GPUParticles2D

func emit_burst() -> void:
	restart()
	emitting = true
	await get_tree().create_timer(lifetime + 0.05).timeout
	if is_instance_valid(self):
		queue_free()
"

[sub_resource type="Gradient" id="grad_hit"]
colors = PackedColorArray(1, 1, 0.7, 1, 1, 0.5, 0.2, 1, 0.7, 0.1, 0.05, 0)

[sub_resource type="ParticleProcessMaterial" id="ppm_hit"]
direction = Vector3(1, 0, 0)
spread = 70.0
initial_velocity_min = 50.0
initial_velocity_max = 110.0
gravity = Vector3(0, 0, 0)
scale_min = 0.6
scale_max = 1.2
color_ramp = SubResource("grad_hit")
damping_min = 80.0
damping_max = 120.0

[node name="HitParticles" type="GPUParticles2D"]
amount = 8
lifetime = 0.25
one_shot = true
explosiveness = 1.0
process_material = SubResource("ppm_hit")
script = SubResource("gd_hit")
